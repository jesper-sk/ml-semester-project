
def make_inferences(weights, start_input, sec_duration):
    inferences = []

    inferenced_time = 0 # In seconds
    while inferenced_time < sec_duration:
        inference = [] # TODO
        inferences.append(inference)
        inferenced_time += inference.get_duration()
    
    return inferences
